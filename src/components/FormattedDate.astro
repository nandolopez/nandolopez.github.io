---
interface Props {
	date: Date;
}

const { date } = Astro.props;
const finalDate = new Date(date)
---

<time datetime={finalDate.toString()} class="w-32">
	{
		finalDate.toLocaleDateString('en-UK', {
			day: 'numeric',
			month: 'long',
			year: 'numeric',
		})
	}
</time>
