---
interface Props {
	date: Date;
}

const { date } = Astro.props;
const finalDate = new Date(date)
---

<time datetime={finalDate.toString()}>
	{
		finalDate.toLocaleDateString('es', {
			day: 'numeric',
			month: 'long',
			year: 'numeric',
		})
	}
</time>
